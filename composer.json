{
    "name": "cursos/certificados-lote",
    "description": "Sistema profissional de geracao automatica de certificados em lote.",
    "type": "project",
    "require": {
        "php": "^8.2",
        "dompdf/dompdf": "^3.1",
        "endroid/qr-code": "^5.0",
        "maennchen/zipstream-php": "^3.1",
        "shuchkin/simplexlsx": "^1.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.5"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        },
        "files": [
            "app/Support/helpers.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "@php -r \"if (!is_dir('storage/logs')) mkdir('storage/logs', 0777, true); if (!is_dir('public/storage/certificados')) mkdir('public/storage/certificados', 0777, true); if (!is_dir('public/uploads')) mkdir('public/uploads', 0777, true);\""
        ],
        "post-update-cmd": [
            "@post-install-cmd"
        ]
    },
    "config": {
        "sort-packages": true,
        "platform": {
            "php": "8.2.12"
        }
    }
}
